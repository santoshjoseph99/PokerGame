// @flow

var Deck = require('./deck');
var Player = require('./player');

class Game {
  type: string;
  blinds: number[];
  deck: Deck;
  players: Player[];

  startGame(){
    
  }
  startHand(){
    
  }
}

module.exports = Game;